---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section
  class:list={[
    "grid min-h-60 w-full grid-cols-1 justify-between gap-6 md:grid-cols-2 xl:grid-cols-3 md:gap-12 xl:gap-8",
    className,
  ]}
>
  <slot />
</section>
