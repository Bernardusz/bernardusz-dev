---
import TitleSection from "@/components/shared/TitleSection.astro";
import projectData from "@/data/projects.json"
import Card from "@/components/shared/Card.astro";
---

<TitleSection title="My Top Projects" flexOrGrid="flex" breakpoint="2xl">
{projectData.top.map(project => (
	<Card
		title={project.title}
		text={project.description}
		iconName={project["icon-name"]}
		iconSize={36}
		class="2xl:max-w-1/4"
  	>
    	<a class="link-primary" href={`projects/${project.slug}`}>Learn More</a>
 	</Card>	
))}
  
</TitleSection>
